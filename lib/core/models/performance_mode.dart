/// Performance mode for the interactive body widget
enum PerformanceMode {
  /// Balanced mode - good performance with full features
  balanced,
  
  /// Fast mode - optimized for performance, may reduce some features
  fast,
  
  /// Quality mode - best visual quality, may be slower
  quality,
}
